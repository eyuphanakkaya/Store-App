language: swift
os: osx
osx_image: xcode14.3

script:
  - xcodebuild clean test \
      -project Store-App/Store-App.xcodeproj \
      -scheme "CI" \
      -destination "platform=iOS Simulator,name=iPhone 14,OS=17.3" \
      -testPlan CITestPlan \
      CODE_SIGN_IDENTITY="" \
      CODE_SIGNING_REQUIRED=NO
